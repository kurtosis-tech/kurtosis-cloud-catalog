{
  "name": "kurtosis-cloud-catalog",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "build/index.d.ts",
  "repository": "https://github.com/kurtosis-tech/kurtosis-cloud-catalog",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "rimraf build && tsc --build",
    "start": "nodemon src/index.ts"
  },
  "private": true,
  "dependencies": {
    "@types/node": "^20.4.8",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node": "^20.5.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.1.6"
  }
}
